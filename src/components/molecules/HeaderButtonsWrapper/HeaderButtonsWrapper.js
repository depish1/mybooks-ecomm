import React from 'react';
import StyledHeaderButtonsWrapper from './HeaderButtonsWrapper.styles';

const HeaderButtonsWrapper = ({ children }) => {
  return <StyledHeaderButtonsWrapper>{children}</StyledHeaderButtonsWrapper>;
};

export default HeaderButtonsWrapper;
